DB_PASSWORD=yourpassword
JWT_SECRET=anylongrandumstring32charsmin
DB_NAME=finance_dashboard

# CORS origins (comma-separated). Defaults to localhost origins if not specified.
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://findash-zorvyn-two.vercel.app